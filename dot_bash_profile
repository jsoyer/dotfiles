#!/usr/bin/env bash
# ============================================================================
# Bash Profile - Login shell configuration
# On macOS, Terminal/iTerm2 start login shells which read this file
# ============================================================================

# Source .bashrc for interactive shell configuration
if [[ -f "${HOME}/.bashrc" ]]; then
  source "${HOME}/.bashrc"
fi
