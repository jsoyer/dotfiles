#!/usr/bin/env bash
# Bash aliases - sources shared aliases + bash-specific additions

SHELL_NAME="bash"

# Source shared aliases (common to bash and zsh)
source "${HOME}/.shell_common/aliases.sh"

# ============================================================================
# Bash-specific aliases (none currently)
# ============================================================================
