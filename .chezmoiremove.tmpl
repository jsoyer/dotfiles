# Leftovers are deleted by run_once scripts, not here.
# chezmoiremove prompts on every interactive `ca` until the path is gone,
# and used to fight .chezmoiignore over ~/.agents (agent tooling still
# uses that directory). Keep this file empty of target paths.
